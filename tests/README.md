# 광동제약 공장 방문예약 시스템 - Playwright 테스트

## 개요

이 디렉토리는 광동제약 공장 방문예약 시스템(https://kwangdong-site.vercel.app/)에 대한 Playwright 자동화 테스트를 포함합니다.

## 📁 테스트 구조

```
tests/
├── homepage/           # 홈페이지 테스트
│   └── homepage-load.spec.ts
├── navigation/         # 네비게이션 테스트
│   └── navigation-menu.spec.ts
├── reservation/        # 예약 신청 테스트
├── progress/           # 예약 현황 테스트
├── employee/           # 임직원 모드 테스트
├── notice/             # 공지사항 테스트
├── faq/               # FAQ 테스트
├── accessibility/      # 접근성 테스트
├── performance/        # 성능 테스트
└── security/           # 보안 테스트
```

## 🚀 빠른 시작

### 필수 조건

- Node.js v18 이상
- npm

### 설치

테스트는 이미 설정되어 있습니다. 프로젝트 루트에서:

```bash
# 의존성이 이미 설치되어 있으면 생략 가능
npm install

# Playwright 브라우저가 설치되어 있지 않으면
npx playwright install
```

## 🧪 테스트 실행

### 모든 테스트 실행

```bash
npm test
```

### 특정 브라우저로 테스트

```bash
# Chromium만
npm run test:chromium

# Firefox만
npm run test:firefox

# WebKit (Safari)만
npm run test:webkit
```

### 특정 테스트 파일 실행

```bash
npm test -- tests/homepage/homepage-load.spec.ts
```

### 헤드 모드 (브라우저 UI 표시)로 실행

```bash
npm run test:headed
```

### 디버그 모드

```bash
npm run test:debug
```

### UI 모드 (대화형 테스트 실행)

```bash
npm run test:ui
```

## 📊 테스트 리포트

### HTML 리포트 보기

테스트 실행 후:

```bash
npm run test:report
```

브라우저에서 자동으로 HTML 리포트가 열립니다.

## ✅ 현재 구현된 테스트

### 홈페이지 테스트 (6개)

- [x] 홈페이지 로드 확인
- [x] 네비게이션 메뉴 표시
- [x] 방문절차 5단계 표시
- [x] 공장 정보 표시
- [x] 신청하기 버튼 동작
- [x] 페이지 로드 성능 (< 3초)

### 네비게이션 테스트 (6개)

- [x] 방문예약 메뉴 클릭
- [x] 예약현황 메뉴 클릭
- [x] 공지사항 메뉴 클릭
- [x] FAQ 메뉴 클릭
- [x] 임직원모드 클릭
- [x] 모든 페이지 일관성 확인

**총 12개 테스트 + 2개 seed 테스트 = 14개 모두 통과 ✅**

## 🗓️ 다음 구현 예정

### Phase 2: 예약 기능 테스트 (예정)

- [ ] 공장 선택 드롭다운
- [ ] 약관 동의 페이지
- [ ] 방문예약 신청 - 정상 경로
- [ ] 방문예약 신청 - 유효성 검사
- [ ] 예약 조회
- [ ] 예약 수정
- [ ] 예약 취소

### Phase 3: 임직원 모드 테스트 (예정)

- [ ] 임직원 로그인
- [ ] 예약 승인/거부
- [ ] 관리자 대시보드

### Phase 4: 품질 속성 테스트 (예정)

- [ ] 접근성 (WCAG 2.1 AA)
- [ ] 반응형 디자인 (모바일, 태블릿, 데스크톱)
- [ ] 성능 메트릭
- [ ] 보안 테스트

## 🛠️ 테스트 작성 가이드

### 기본 테스트 구조

```typescript
import { test, expect } from '@playwright/test';

test.describe('테스트 그룹명', () => {
  test('테스트 케이스 설명', async ({ page }) => {
    // 페이지 이동
    await page.goto('/');

    // 요소 확인
    await expect(page.getByRole('heading')).toBeVisible();

    // 인터랙션
    await page.getByRole('button', { name: '클릭' }).click();

    // 검증
    await expect(page).toHaveURL('/expected-url');
  });
});
```

### 권장 로케이터 사용법

1. **Role 기반** (최우선)
   ```typescript
   page.getByRole('button', { name: '신청하기' })
   page.getByRole('link', { name: '예약현황' })
   ```

2. **Label 기반** (폼 필드)
   ```typescript
   page.getByLabel('이름')
   page.getByLabel('연락처')
   ```

3. **Text 기반** (읽기 전용 텍스트)
   ```typescript
   page.getByText('방문예약시스템에 오신 것을 환영합니다')
   ```

4. **Strict Mode 주의**
   - 같은 텍스트가 여러 곳에 있으면 `.first()` 사용
   - 또는 더 구체적인 로케이터 사용 (예: `nav.getByRole(...)`)

### 한글 사용 시 주의사항

- 테스트 설명과 로케이터에 한글 사용 가능
- UTF-8 인코딩 확인
- `locale: 'ko-KR'` 설정 (playwright.config.ts에 이미 설정됨)

## 🐛 트러블슈팅

### 테스트가 타임아웃되는 경우

```typescript
test('느린 작업', async ({ page }) => {
  test.setTimeout(60000); // 60초로 증가
  // ... 테스트 코드
});
```

### 브라우저가 설치되지 않았다는 오류

```bash
npx playwright install chromium
```

### 스크린샷이 필요한 경우

```bash
npm test -- --screenshot=on
```

### 디버깅이 필요한 경우

```bash
# 특정 테스트만 디버그
npm test -- tests/homepage/homepage-load.spec.ts --debug

# 또는 VSCode에서 디버깅 설정 사용
```

## 📝 테스트 데이터

테스트 데이터는 각 테스트 파일 내에 하드코딩되어 있습니다.
향후 별도의 fixtures 디렉토리로 분리 예정입니다.

## 🔧 설정 파일

### [playwright.config.ts](../playwright.config.ts)

- **baseURL**: `https://kwangdong-site.vercel.app`
- **timeout**: 30초
- **retries**: CI에서만 2회
- **browsers**: Chromium, Firefox, WebKit
- **mobile**: Pixel 5, iPhone 12
- **tablet**: iPad Pro
- **locale**: ko-KR
- **timezone**: Asia/Seoul

## 📈 테스트 결과

마지막 실행 결과 (2026-01-01):

```
14 passed (58.2s)
- 홈페이지 테스트: 6/6 통과
- 네비게이션 테스트: 6/6 통과
- Seed 테스트: 2/2 통과
```

## 🤝 기여 가이드

새로운 테스트를 추가하려면:

1. 적절한 디렉토리에 `.spec.ts` 파일 생성
2. 테스트 플랜([specs/kwangdong-visitor-test-plan.md](../specs/kwangdong-visitor-test-plan.md)) 참조
3. 권장 로케이터 사용
4. 테스트 실행하여 통과 확인
5. 이 README 업데이트

## 📚 참고 자료

- [Playwright 공식 문서](https://playwright.dev/)
- [Playwright Best Practices](https://playwright.dev/docs/best-practices)
- [테스트 플랜 문서](../specs/kwangdong-visitor-test-plan.md)

## 📞 문의

테스트 관련 문의사항은 프로젝트 담당자에게 연락하세요.

---

**마지막 업데이트**: 2026-01-01
**Playwright 버전**: 1.57.0
